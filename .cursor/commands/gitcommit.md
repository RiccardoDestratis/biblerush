# gitcommit


first build the app to ensure we don thave any errors
come up with a nice commit message not too long not too short, that describes the latest changes
if no errors you can push 
you need to clear the cache restart the dev server afterwords by executing restart-dev.sh
